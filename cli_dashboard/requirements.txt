# CLI Dashboard Requirements for ISH Chat

# Core dependencies
aiohttp>=3.8.0
asyncio-throttle>=1.0.2

# Rich terminal UI (optional but recommended)
rich>=13.0.0
textual>=0.41.0

# Data handling
pydantic>=2.0.0
python-dateutil>=2.8.0

# Configuration
pyyaml>=6.0
python-dotenv>=1.0.0

# Logging and debugging
colorlog>=6.7.0

# Development dependencies (optional)
pytest>=7.0.0
pytest-asyncio>=0.21.0
black>=23.0.0
flake8>=6.0.0
mypy>=1.0.0

# Optional: For enhanced terminal features
click>=8.1.0
tabulate>=0.9.0